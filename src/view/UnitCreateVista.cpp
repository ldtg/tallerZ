#include "UnitCreateVista.h"

UnitCreateVista::UnitCreateVista(const char *file, int num_frames, int speed)
    : ExplosionVista(file, num_frames, speed) {}

void UnitCreateVista::finishExplosion(View *view) {
//  std::string rotation_s("270");
//  std::string action("create");
//  ObjectMapaVista *unitVista = view->getUnitVista(id.getType(), action, rotation_s);
//  unitVista->setPos(pos);

//  view->addUnitVista(id, unitVista);
}
