#include "GameCotroller.h"
#include "AStar.h"
/*void GameCotroller::move(IdUnit idunit, Position position) {
  Unit *unit = units[idunit];
  AStar astar(map, unit, map.getTile(position));
  unit->setMovements(astar.find());
}*/
