#include "Event.h"

Event::Event(Model &model, View &view) : model(model), view(view) {}

void Event::setModel(Model &model) {
//  this->model = model;
}

void Event::setView(View &view) {
//  this->view = view;
}
