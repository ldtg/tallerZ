#include "BulletMoveStepEvent.h"

BulletMoveStepEvent::BulletMoveStepEvent(BulletID id, Position &posTo)
    : id(id), posTo(posTo) {}

void BulletMoveStepEvent::process() {
//  ObjectMapaVista *bulletVista = view->getBulletVista(id);
//  bulletVista->setPos(posTo);
}
